{
  "name": "opencode-local-model-discovery-plugin",
  "version": "0.1.0",
  "description": "OpenCode plugin that auto-discovers models from OpenAI-compatible local providers",
  "type": "module",
  "license": "MIT",
  "main": "opencode-local.ts",
  "exports": {
    ".": "./opencode-local.ts"
  },
  "files": ["opencode-local.ts", "opencode-local-model"],
  "scripts": {
    "lint": "biome check .",
    "format": "biome check --write .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@opencode-ai/plugin": "^0.4.0",
    "typescript": "^5.7.0"
  },
  "trustedDependencies": ["@biomejs/biome"]
}
